<!DOCTYPE html>
<html>
  <head>
    <title>AwsUploader</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>

    <nav class="navbar navbar-dark bg-dark">
    <a class="navbar-brand" href="/"><%= image_tag("AWS.svg", :class => "logo") %></a>
    <a class="navbar-brand" href="/">Scranton Gillete S3 Uploader</a>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item active">
          <a class="nav-link" href="#">Home <span class="sr-only">(current)</span></a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#">Features</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#">Pricing</a>
        </li>
        <li class="nav-item">
          <a class="nav-link disabled" href="#">Disabled</a>
        </li>
      </ul>
    </div>
  </nav>
    <div class="yield-wrapper">
      <%= form_tag @post.url, class: "dropzone", id: "uploader" do %>
        <div class="dz-message" data-dz-message><%= image_tag("upload2.svg", :class => "upload") %></div>
        <% @post.fields.each do |key, value| %>
          <%= hidden_field_tag key, value %>
        <% end %>
      <% end %>
      <br>
      <%= yield %>
    </div>
  </body>
</html>
